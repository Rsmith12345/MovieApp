1. Make sure node_modules is installed in frontend.
2. Create a venv in backend with necessary libraries installed (use python 3.13 interpreter)
3. run python app.py in one terminal in backend folder (also with activated venv)
4. run npm start in another terminal in frontedn folder
